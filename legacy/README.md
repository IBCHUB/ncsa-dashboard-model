This directory contains archived repo-level components that are not part of the active runtime.

- `dashboard-poc/` is the original Next.js proof-of-concept dashboard.
- Active ownership in this repo is currently `ai-service/`, its warehouse pipeline, and supporting docs/config.
- Keep imports and compose/runtime paths pointed at active modules, not archived code.
