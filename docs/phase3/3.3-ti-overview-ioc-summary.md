# 3.3 — Threat Intelligence (Overview + IOC Summary tabs)

## หน้าเว็บที่ตรวจ

- **ชื่อหน้า:** Threat Intelligence (มี 2 tabs)
- **URL:** `/admin/threatintelligence?tab=overview` และ `/admin/threatintelligence?tab=ioc`
- **ผู้ใช้หลัก:** Security Analyst / TI Analyst — มอง Threat overview + สถิติการ import IOC

## API endpoints ที่หน้านี้เรียก (checklist)

### Tab: Overview

- [x] `GET /api/v1/executive/dashboard` — ใช้แสดงทุก KPI cards + Threat Volume Trend + Attack Origins map + Sector treemap (reuse จาก Phase 3.1 ✅)
- [x] `GET /api/v1/operations/dashboard` — ใช้แสดง Attack Time heatmap + Top Threat Types + Top Intelligence Sources (reuse จาก Phase 3.2 ✅)

### Tab: IOC Summary

- [x] `GET /api/v1/ioc-analytics?tab=ioc-summary` — Summary section (4 KPI + charts) — warehouse `.43`
- [x] `GET /api/v1/ioc-analytics?tab=statistics-import` — Import section (4 KPI + charts) — datalake `.41` primary, warehouse `.43` fallback

รวม **4 endpoint calls** (2 endpoints, แต่ ioc-analytics ถูกเรียก 2 ครั้งด้วย tab ต่างกัน), ตรวจแล้ว **4/4** ✅

## รายการตัวเลข/chart ที่ตรวจ

### 🏷️ Tab: Overview — KPI Cards (5 ตัว)

| Label บนหน้าเว็บ | ที่แสดงเป็นอะไร | API endpoint | สูตรคำนวณ (ภาษาคน) | แหล่งข้อมูล | ถูกไหม | หมายเหตุ |
|----------------|----------------|--------------|--------------------|------------|--------|---------|
| **Worldwide Threat** | จำนวนภัยคุกคามทั่วโลก + delta % | `GET /executive/dashboard` | นับ doc ทั้งหมดในช่วงเวลา | warehouse `.43` | ✅ | เดียวกับ Executive (Phase 3.1) |
| **Thailand Threat** | จำนวนภัยคุกคามที่กระทบไทย + delta % | `GET /executive/dashboard` | นับ doc ที่ geo_country=TH | warehouse `.43` | ✅ | เดียวกับ Executive (Phase 3.1) |
| **Active IOC** | จำนวน unique IOC ที่ active + delta % | `GET /executive/dashboard` | cardinality canonical_ioc_key | warehouse `.43` | ✅ | ใช้ `exposure_today.ioc_active` — field name ตรงกับ label |
| **Critical IOCs** | จำนวน unique critical IOC + delta % | `GET /executive/dashboard` | filter severity=critical แล้ว cardinality | warehouse `.43` | ✅ | ใช้ `exposure_today.critical_active` — ตรงกับ label |
| **High IOCs** | จำนวน unique high IOC + delta % | `GET /executive/dashboard` | filter severity=high แล้ว cardinality | warehouse `.43` | ✅ | data ปัจจุบัน high แทบ 0 |

### 📊 Tab: Overview — Charts

| Label บนหน้าเว็บ | ที่แสดงเป็นอะไร | API endpoint | สูตรคำนวณ | แหล่งข้อมูล | ถูกไหม |
|----------------|----------------|--------------|----------|------------|--------|
| **Threat Volume Trend** | Line chart 3 เส้น (Total / Critical / High) + Forecast | `GET /executive/dashboard` | date_histogram + severity sub-aggs | warehouse `.43` | ✅ (Phase 3.1) |
| **Attack Time Heatmap** | Heatmap day × hour | `GET /operations/dashboard` | date_histogram bucket 3h × day-of-week | warehouse `.43` | ✅ (Phase 3.2) |
| **Attack Origins** | Geo map / list | `GET /executive/dashboard` | top 10 ของ geo_country | warehouse `.43` | ⚠️ coverage 0.4% |
| **Top Threat Types** | Bar/donut | `GET /operations/dashboard` | top 5 ของ ai_threat_types | warehouse `.43` | ⚠️ vocabulary ปนกัน (Phase 1 issue) |
| **Threat Volume & Severity Analysis** (treemap) | Sector treemap | `GET /executive/dashboard` | top 12 target_sector_name + severity sub-aggs | warehouse `.43` | ⚠️ coverage 2% |
| **Threat Intelligence Source** | Bar chart | `GET /operations/dashboard` | top 5 ของ source_name | warehouse `.43` | ⚠️ cyberint 99.99% |

### 🏷️ Tab: IOC Summary — Summary Section (4 KPI cards)

| Label บนหน้าเว็บ | ที่แสดงเป็นอะไร | API endpoint | สูตรคำนวณ (ภาษาคน) | แหล่งข้อมูล | ถูกไหม | หมายเหตุ |
|----------------|----------------|--------------|--------------------|------------|--------|---------|
| **Total IOCs** | จำนวน IOC รวมทั้งหมด | `GET /ioc-analytics?tab=ioc-summary` | นับ doc ทั้งหมด (`aggs.total`) | warehouse `.43`, time_mode=observed | ✅ | ใช้ event_time/first_seen/last_seen filter |
| **New IOCs** | จำนวน unique IOC ในช่วงเวลา | `GET /ioc-analytics?tab=ioc-summary` | cardinality canonical_ioc_key | warehouse `.43` | ✅ | alias ของ active_ioc — backend ตั้งเหมือนกัน |
| **Critical IOCs** | จำนวน unique critical IOC | `GET /ioc-analytics?tab=ioc-summary` | filter severity=critical → cardinality | warehouse `.43` | ✅ | คนละค่ากับ `critical_ioc_docs` (doc count) |
| **AVG Risk Score** | คะแนนความเสี่ยงเฉลี่ย (out of 100) | `GET /ioc-analytics?tab=ioc-summary` | avg ของ `ai_risk_score` | warehouse `.43` | ✅ | data ปัจจุบัน 7d window = 29.2 (medium-low) |

### 📊 Tab: IOC Summary — Summary Charts

| Label บนหน้าเว็บ | ที่แสดงเป็นอะไร | API endpoint | สูตรคำนวณ | แหล่งข้อมูล | ถูกไหม |
|----------------|----------------|--------------|----------|------------|--------|
| **IOC Type** (donut) | distribution ตาม ioc_type | `GET /ioc-analytics?tab=ioc-summary` | terms agg บน ioc_type | warehouse `.43` | ✅ |
| **Severity** (donut) | distribution ตาม severity | `GET /ioc-analytics?tab=ioc-summary` | filters agg 5 buckets | warehouse `.43` | ✅ |
| **Threat Type** (donut) | distribution ตาม threat type | `GET /ioc-analytics?tab=ioc-summary` | terms agg บน ai_threat_types | warehouse `.43` | ⚠️ vocabulary ปนกัน |
| **Severity by Type** (matrix) | severity × ioc_type table | `GET /ioc-analytics?tab=ioc-summary` | terms (ioc_type) × filters (severity) | warehouse `.43` | ✅ |
| **Risk Score Distribution** (bar) | 5 buckets: 0-19/20-39/40-59/60-79/80-100 | `GET /ioc-analytics?tab=ioc-summary` | range agg บน ai_risk_score | warehouse `.43` | ✅ |

### 🏷️ Tab: IOC Summary — Import Section (4 KPI cards)

| Label บนหน้าเว็บ | ที่แสดงเป็นอะไร | API endpoint | สูตรคำนวณ (ภาษาคน) | แหล่งข้อมูล | ถูกไหม | หมายเหตุ |
|----------------|----------------|--------------|--------------------|------------|--------|---------|
| **Total IOC Imported** | จำนวน IOC ที่ import ทั้งหมด | `GET /ioc-analytics?tab=statistics-import` | นับ doc ทั้งหมด (`aggs.total`) | datalake `.41` (primary) → warehouse `.43` (fallback ถ้า datalake=0) | ✅ | datalake = raw feed |
| **Average per Day** | จำนวนเฉลี่ยต่อวัน (ปัดเศษ) | `GET /ioc-analytics?tab=statistics-import` | total / active_days (วันที่มีค่า > 0) | datalake/warehouse | ✅ | frontend ปัดเศษอีกที (Math.round) |
| **Top Source** | source ที่ import มากสุด (text) | `GET /ioc-analytics?tab=statistics-import` | top 1 ของ source terms → label format | datalake/warehouse | ✅ | คนละค่ากับ Operations "Top Intelligence Sources" (warehouse) |
| **Data Quality Score** | % คุณภาพข้อมูล | `GET /ioc-analytics?tab=statistics-import` | (quality_complete / total) × 100 | datalake/warehouse | ✅ | quality = docs ที่ field ครบ (ioc_value+ioc_type สำหรับ datalake / 4 fields สำหรับ warehouse) |

### 📊 Tab: IOC Summary — Import Charts

| Label บนหน้าเว็บ | ที่แสดงเป็นอะไร | API endpoint | สูตรคำนวณ | แหล่งข้อมูล | ถูกไหม |
|----------------|----------------|--------------|----------|------------|--------|
| **Import Volume Over Time** | Line chart timeline | `GET /ioc-analytics?tab=statistics-import` | date_histogram day buckets + source category split | datalake/warehouse | ✅ |
| **Intelligence Source** (bar) | distribution ตาม source | `GET /ioc-analytics?tab=statistics-import` | terms agg บน source_name | datalake/warehouse | ⚠️ cyberint dominant |

## Bugs ที่พบ

| # | Severity | Label ที่กระทบ | API endpoint | ปัญหา | สถานะ |
|---|----------|---------------|--------------|------|------|
| _(ไม่มี bug ใหม่ใน Phase 3.3)_ | — | — | — | TI Overview reuse Executive + Operations endpoints ที่ label↔value mapping ถูกแล้ว (Executive ใช้ `exposure_today.ioc_active`/`critical_active`/`high_active` ที่ตรงกับ label "Active IOC"/"Critical IOCs"/"High IOCs") | — |

### Bug จาก Phase 3.0/3.2 ที่กระทบหน้านี้ (แก้แล้ว)

| # | Severity | Label ที่กระทบ | สถานะ |
|---|----------|---------------|------|
| BUG-3.0-5 | HIGH | Overview KPI cards (รวม Executive ทั้งหมด) | ✅ แก้แล้ว Phase 3.0 |
| BUG-3.0-7 | MED | Thailand Threat badge | ✅ แก้แล้ว Phase 3.0 |
| BUG-3.2-1 | CRITICAL | _ไม่กระทบ_ | TI Overview ใช้ Executive endpoint ไม่ใช่ Operations endpoint ที่ผิด — TI หน้านี้ปลอดภัยตั้งแต่เดิม |

## ข้อสังเกต / Data quality

| # | ประเด็น | ผลต่อหน้าเว็บ |
|---|--------|---------------|
| 1 | **audit-map.yaml อ้าง successful_import/failed_import hardcoded — ความจริงไม่ใช่** | Backend คำนวณ dynamic ผ่าน `quality_complete` agg (field exists check). Audit-map ผิด — update ใน docs ได้ |
| 2 | IOC Summary tab ใช้ `time_mode=observed` (event_time) — Operations ใช้ `processed` (processed_at) | ตัวเลข total 7d ต่างกัน: observed = 24,497 vs processed = 276,228 — เพราะ event time คือเวลาเกิดเหตุจริง doc ส่วนใหญ่มี event_time เก่ากว่า 7 วัน |
| 3 | warehouse quality_complete strict กว่า datalake (4 fields exist vs 2) | ถ้า fallback ไป warehouse — failed_import จะมากกว่า datalake primary path |
| 4 | data ปัจจุบัน warehouse 7d window มี **0 critical docs** (event_time filter) | "Critical IOCs" บน IOC Summary tab จะแสดง 0 — ไม่ใช่ bug แต่ data ใน window ที่เลือก |
| 5 | data 7d (observed): avg_risk_score = 29.2 | "AVG Risk Score" จะแสดง ~29/100 |
| 6 | TI Overview tab reuse 2 endpoints — load 2 requests ขนาน (Promise.all) | latency = max(executive, operations) ไม่ใช่ sum — OK |
| 7 | "Top Source" บน Import section, format pretty ที่ frontend (`cyberint_iocs` → `Cyberint IOCs`) | display name ต่างจาก raw value แต่ถูกแปลง |

## Spot-check จาก ES `.43` จริง (warehouse, observed mode, 7d)

```
total_ioc         (= aggs.total)                 = 24,497
active_ioc/new_ioc (cardinality unique IOC)      = 24,427
critical_ioc      (cardinality critical unique)  = 0
critical_ioc_docs (doc count critical)           = 0
clean_ioc         (severity.clean doc count)     = 0
risk_ioc          (ai_risk_score >= 80)          = 0
avg_risk_score    (avg ai_risk_score)            = 29.2

quality_complete  (4 fields exist - warehouse)   = 24,497
failed_import (= total - quality)                = 0
data_quality_score                               = 100%
```

ทุกสูตรคำนวณถูกต้องตาม code

## Regression tests

ยังไม่มี — Phase 3.3 ไม่พบ bug ใหม่ การแก้จาก Phase 3.0/3.2 ครอบคลุมหน้านี้แล้ว

## Commits ที่เกี่ยวข้อง

- Phase 3.0: `1c55556`, `a578e50`
- Phase 3.1: `cfa1334`, `bd242f4` (Executive — reuse โดย TI Overview tab)
- Phase 3.2: `bd7d675` (Operations — reuse โดย TI Overview tab)
- Phase 3.3: _ไม่มี commit fix_ — เป็นการ audit + document mapping เท่านั้น
