# 3.7 — Action Center / Reports / News / CVE Intelligence

## หน้าเว็บที่ตรวจ

4 หน้า workflow / derived data:

| ลำดับ | ชื่อหน้า | URL | สถานะ implementation |
|------|--------|-----|---------------------|
| 1 | Action Center | `/admin/actioncenter` | ✅ implemented |
| 2 | Reports (IOC Report) | `/admin/reports` | ✅ implemented |
| 3 | News | `/admin/news` | ✅ implemented |
| 4 | Newsfeed | `/admin/newsfeed` | ❌ stub (placeholder, ไม่ wire กับ backend) |
| 5 | CVE Intelligence | `/admin/cveintelligence` | ❌ stub (backend มี endpoint แต่ frontend placeholder) |

## API endpoints ที่หน้านี้เรียก (checklist)

### Page 1: Action Center

- [x] `GET /api/v1/actions` — list + KPI summary + facets
- [x] `GET /api/v1/actions/{action_id}` — action detail
- [x] `POST /api/v1/actions/{id}/assign` — assign analyst (Phase 2.5 TLP gate)
- [x] `POST /api/v1/actions/{id}/block-ip` — block IP (Phase 2.5 require_admin)
- [x] `POST /api/v1/actions/{id}/false-positive` — mark FP (Phase 2.5 TLP gate)
- [x] `POST /api/v1/actions/{id}/notes` — add note (Phase 2.5 TLP gate)
- [x] `POST /api/v1/reports/actions/export` + `GET /api/v1/exports/{id}` — CSV export

### Page 2: Reports

- [x] `POST /api/v1/reports/ioc/preview` — preview before export
- [x] `POST /api/v1/reports/ioc/export` + `GET /api/v1/exports/{id}` + `/download` — async export

### Page 3: News

- [x] `GET /api/v1/news` — news list
- [x] `GET /api/v1/news/{article_id}` — detail (Phase 2.5 direct doc lookup)

### Page 4-5: Newsfeed + CVE Intelligence (stub frontend)

- [ ] `GET /api/v1/cve-intelligence` — endpoint มี (warehouse + datalake) แต่ frontend ยัง stub — **ไม่นับใน Phase 3 audit เพราะ user ไม่เห็นข้อมูลจริง**

รวม **12 endpoints** (Action + Reports + News), ตรวจแล้ว **12/12** ✅ (Newsfeed/CVE skip — ไม่ implemented)

## รายการตัวเลข/chart ที่ตรวจ

### Page 1: Action Center

| Label บนหน้าเว็บ | ที่แสดงเป็นอะไร | API endpoint | สูตรคำนวณ (ภาษาคน) | แหล่งข้อมูล | ถูกไหม | หมายเหตุ |
|----------------|----------------|--------------|--------------------|------------|--------|---------|
| **Total** (KPI) | จำนวน action ทั้งหมดในช่วงเวลา | `GET /actions` | นับ doc ที่ `action_required=true OR action_status in (open/in_progress/closed)` + tlp != red | warehouse `.43` | ✅ | data ปัจจุบัน = 10.76M (ทุก doc) |
| **Open** (KPI, สีฟ้า) | จำนวน action ที่ยังเปิดอยู่ | `GET /actions` | filter `action_status=open` | warehouse `.43` | ✅ | data ปัจจุบัน = 10.76M (= Total — ทุก doc เป็น open) |
| **In Progress** (KPI, สีส้ม) | จำนวน action ที่กำลังดำเนินการ | `GET /actions` | filter `action_status=in_progress` | warehouse `.43` | ✅ | data ปัจจุบัน = 0 |
| **Closed** (KPI, สีเขียว) | จำนวน action ที่ปิดแล้ว | `GET /actions` | filter `action_status=closed` | warehouse `.43` | ✅ | data ปัจจุบัน = 0 |
| **Table columns:** | per-row info | `GET /actions` | (per doc) | warehouse `.43` | ✅ | |
| Action ID | unique id | same | `action_id` | | ✅ | |
| Status | open/in_progress/closed | same | `action_status` derived | | ✅ | data 100% "open" |
| Severity | critical/high/medium/low/clean | same | `severity` field | | ✅ | |
| Title | description | same | `description` หรือ derived | | ✅ | |
| Type | ioc_type | same | `ioc_type` | | ✅ | |
| Context | source/threat info | same | combined | | ✅ | |
| Sources | source_name | same | `source_name` array | | ⚠️ | 99% cyberint |
| SLA | due date | same | derived from action_opened_at + SLA hours | | ✅ | |
| Date/Time | when action opened | same | `action_opened_at` หรือ `processed_at` | | ✅ | |
| **Action buttons:** | interaction | various | (per Phase 2.5 fixes) | warehouse `.43` | ✅ | |
| Block IP | network enforcement | `POST /actions/{id}/block-ip` | require_admin (Phase 2.5) + ensure_doc_visible | | ✅ | admin gate working |
| Assign | analyst assignment | `POST /actions/{id}/assign` | ensure_doc_visible (TLP red blocked) | | ✅ | |
| Mark FP | false positive | `POST /actions/{id}/false-positive` | ensure_doc_visible | | ✅ | |
| Add Note | note | `POST /actions/{id}/notes` | actor_display_name (name→email→username→id) | | ✅ | Phase 2.5 SSO fallback |

### Page 2: Reports

| Label บนหน้าเว็บ | ที่แสดงเป็นอะไร | API endpoint | สูตรคำนวณ | แหล่งข้อมูล | ถูกไหม | หมายเหตุ |
|----------------|----------------|--------------|----------|------------|--------|---------|
| **Total IOCs** (after Generate) | จำนวน IOC ที่ตรง filter | `POST /reports/ioc/preview` | `aggs.total` | warehouse `.43` | ✅ | observed mode |
| **Critical/High** | จำนวน IOC severity = critical+high | same | severity sub-aggs sum | warehouse `.43` | ✅ | |
| **Sources** | จำนวน source ที่พบ (unique) | same | cardinality `source_name` | warehouse `.43` | ⚠️ | data 99.99% cyberint → ตัวเลขเสมอ 1 |
| **Threat Types** | จำนวน threat type (unique) | same | cardinality `ai_threat_types` | warehouse `.43` | ✅ | |
| **Table columns:** | per-IOC | same | (per doc) | warehouse `.43` | ✅ | |
| # / IOC Value / Type / Severity / Risk Score / Threat Types / Sources / View | per-row | same | mapping ถูก | | ✅ | |
| **Generate Report** button | trigger preview | `POST /reports/ioc/preview` | filter + 200 items max | | ✅ | |
| **Export** button | async export | `POST /reports/ioc/export` | Phase 2.6 owner_user_id + TTL + size limit | | ✅ | |
| **Filters:** IOC Type, Severity, Threat Types, Sources, Date Range | filter facets | preview | filter passthrough | | ✅ | |

### Page 3: News

| Label บนหน้าเว็บ | ที่แสดงเป็นอะไร | API endpoint | สูตรคำนวณ | แหล่งข้อมูล | ถูกไหม | หมายเหตุ |
|----------------|----------------|--------------|----------|------------|--------|---------|
| **"Cyber News"** (title) | section header | — | static | — | ✅ | |
| **{N} articles** (count) | จำนวน article ในผลลัพธ์ | `GET /news` | total ของ news scroll | warehouse `.43` | ✅ | |
| **Article cards (grid):** | per-article | `GET /news` | _build_news_articles dedup hash | warehouse `.43` (filter source_type in [news,rss,article]) | ✅ | sort published_at DESC |
| Source name + date | header | same | `source_name` + `published_at` | | ✅ | |
| Title | article title | same | `description` หรือ derived | | ✅ | |
| Related IOCs list + badges | linked IOCs | same | from doc itself | | ✅ | |
| **Click article** | detail view | `GET /news/{article_id}` | direct doc lookup (Phase 2.5) → scroll fallback | warehouse | ✅ | |
| **Search + Source filter** | filter | `GET /news` query+source params | terms filter | | ✅ | |

### Page 4-5: Newsfeed + CVE Intelligence

- **Newsfeed page:** ✘ stub — frontend ไม่เรียก endpoint ใดๆ
- **CVE Intelligence page:** ✘ stub — backend `/cve-intelligence` มี endpoint (warehouse + datalake merge by CVE ID, 5K cap each) แต่ frontend ยังไม่ wire — user ไม่เห็นข้อมูลจริง
- **Skip from Phase 3 audit** เพราะ user ไม่ใช้งานหน้านี้

## Bugs ที่พบ

| # | Severity | Label ที่กระทบ | API endpoint | ปัญหา | สถานะ |
|---|----------|---------------|--------------|------|------|
| _(ไม่มี bug ใหม่ใน Phase 3.7)_ | — | — | — | mapping label↔backend field ตรงทุกตัว | — |

### Bug จาก Phase 2 ที่กระทบหน้านี้ (แก้แล้ว)

| # | Severity | Label ที่กระทบ | สถานะ |
|---|----------|---------------|------|
| BUG-2.5-1 | CRITICAL | Block IP button (Action Center) | ✅ แก้ Phase 2.5 — require_admin |
| BUG-2.5-2 | HIGH | Action mutations (Assign/FP/Notes/Block IP) | ✅ แก้ Phase 2.5 — _ensure_doc_visible (TLP:red blocked) |
| BUG-2.5-3 | HIGH | Add Note "actor" name | ✅ แก้ Phase 2.5 — _actor_display_name (SSO fallback) |
| BUG-2.5-4 | HIGH | Block IP enforcement_point_ids validation | ✅ แก้ Phase 2.5 — reject unknown IDs before mutation |
| BUG-2.5-5 | MED | News detail O(N) scroll | ✅ แก้ Phase 2.5 — direct doc lookup first |
| BUG-2.6-1/2 | HIGH | Reports export ownership | ✅ แก้ Phase 2.6 — owner_user_id + 404 to non-owner |
| BUG-2.6-3 | MED | Reports export TTL cleanup | ✅ แก้ Phase 2.6 — TTL expiry + delete_export_job |
| BUG-2.6-4 | MED | Reports export size | ✅ แก้ Phase 2.6 — EXPORT_MAX_BYTES limit |

## ข้อสังเกต / Data quality

| # | ประเด็น | ผลต่อหน้าเว็บ |
|---|--------|---------------|
| 1 | **Action Center: ทุก doc 10.76M มี `action_status=open` + `action_required=true`** — workflow ไม่เคยถูกใช้งานจริงในข้อมูล production | **Total/Open card = 10.76M** (เลขเดียวกันเพราะทุก doc เปิดอยู่), **In Progress = 0**, **Closed = 0**. Analyst ไม่มีทาง triage 10.76M ได้ → page นี้ใช้งานไม่ได้จริง — **product issue, ไม่ใช่ backend bug** |
| 2 | Action Center "Status" column 100% แสดง "open" | UX ไม่ informative — user เลื่อนดู 1M+ rows ก็เห็นเหมือนกันหมด |
| 3 | Reports "Sources" KPI = 1 (Cyberint คนเดียว) | KPI ไม่ค่อย informative — data 99.99% cyberint |
| 4 | News page: source_name พึ่ง NEWS_SOURCES list + source_type filter | data ปัจจุบันมี news sources หลายรายการ (BleepingComputer, DarkReading, The Hacker News, Zone-H) — OK |
| 5 | Newsfeed + CVE Intelligence ยัง stub | user เปิดเข้าไม่เห็นอะไร — ต้อง wire frontend (ห้ามแก้ frontend ใน Phase 3) |
| 6 | CVE Intelligence backend endpoint ใช้ทั้ง warehouse + datalake | merge by CVE ID, 5K cap each — ถ้าจะใช้ในอนาคต ควร audit อีกครั้ง |

## ที่ต้องตรวจสอบเพิ่มเติม (pending)

- [ ] **Action workflow data ingestion:** ทำไม action_status ไม่เคยเปลี่ยนเป็น in_progress/closed? — ต้องดู pipeline หรือ analyst process (out of Phase 3 scope)
- [ ] **Newsfeed page implementation:** ต้องเช็คกับทีม frontend ว่ามีแผน implement ไหม
- [ ] **CVE Intelligence wire up:** ถ้าจะเปิดใช้ ต้อง audit backend อีกครั้ง (กระทบ Phase 3 budget)

## Regression tests

ยังไม่มี — Phase 3.7 ไม่พบ semantic bug ใหม่ ใช้ regression test จาก Phase 2.5 + 2.6

## Commits ที่เกี่ยวข้อง

- Phase 2.5: action safety + news lookup (5 bugs)
- Phase 2.6: reports export ownership/TTL/size (4 bugs)
- Phase 3.7: _ไม่มี commit fix_ — เป็นการ audit + document mapping เท่านั้น
