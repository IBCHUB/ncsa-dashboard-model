# 3.6 — TI Sub-pages (6 หน้า)

## หน้าเว็บที่ตรวจ

6 หน้า report ใต้ Threat Intelligence (รวมเป็น batch เพราะใช้ endpoint เดียวกัน):

| ลำดับ | ชื่อหน้า | URL |
|------|--------|-----|
| 1 | Analysis Report (Attack Time) | `/admin/threatintelligence/analysisreport` |
| 2 | Threat Trend | `/admin/threatintelligence/threattrend` |
| 3 | Attack Origin | `/admin/threatintelligence/attackorigin` |
| 4 | Threat Type | `/admin/threatintelligence/threattype` |
| 5 | Sector Threat | `/admin/threatintelligence/sectorthreat` |
| 6 | Intelligence Source | `/admin/threatintelligence/intelligencesource` |

## API endpoints ที่หน้านี้เรียก (checklist)

| Endpoint | Method | ใช้บนหน้า |
|----------|--------|----------|
| - [x] `GET /api/v1/operations/reports/attack-time` | GET | Page 1 (Analysis Report) — heatmap + events |
| - [x] `GET /api/v1/threat-intelligence/trend/events` | GET | Page 2 (Threat Trend) — Raw Volume Logs table |
| - [x] `GET /api/v1/executive/dashboard` | GET | Page 2, 3 (trend chart, attack origin map) — reuse Phase 3.1 ✅ |
| - [x] `GET /api/v1/operations/reports/attack-origins` | GET | Page 3 (Attack Origin) — ranking |
| - [x] `GET /api/v1/operations/reports/threat-types` | GET | Page 4 (Threat Type) — list |
| - [x] `GET /api/v1/operations/reports/threat-types/{threat_type:path}` | GET | Page 4 — detail drill-down |
| - [x] `GET /api/v1/operations/reports/target-sectors` (alias `sector`) | GET | Page 5 (Sector Threat) — ranking |
| - [x] `GET /api/v1/operations/reports/intelligence-sources` | GET | Page 6 (Intelligence Source) — performance table |
| - [x] `GET /api/v1/lookups/sectors`, `/lookups/threat-types` | GET | Page 5 — dropdowns |

รวม **9 endpoint signatures** (พร้อม alias) ตรวจแล้ว **9/9** ✅

> **Pattern:** ทุก dimensional report ใช้ endpoint generic `/operations/reports/{report_key}` ผ่าน `REPORT_KEY_ALIASES` map (intelligence-sources / threat-types / attack-origins / target-sectors) — backend ใช้ warehouse `.43` ทั้งหมด, ไม่ใช้ datalake

## รายการตัวเลข/chart ที่ตรวจ

### Page 1: Analysis Report (Attack Time)

| Label บนหน้าเว็บ | ที่แสดงเป็นอะไร | API endpoint | สูตรคำนวณ (ภาษาคน) | ถูกไหม | หมายเหตุ |
|----------------|----------------|--------------|--------------------|--------|---------|
| **Peak Attack Time** | ช่วงเวลาที่ภัยคุกคามสูงสุด | `GET /operations/reports/attack-time` | bucket max ของ heatmap | ✅ | |
| **Quietest Period** | ช่วงเวลาเงียบ | `GET /operations/reports/attack-time` | bucket min | ✅ | |
| **AVG Attack Rate** | อัตราเฉลี่ย | `GET /operations/reports/attack-time` | total / interval count | ✅ | |
| **Highest Day** | วันที่มี attack มากสุด | `GET /operations/reports/attack-time` | day bucket max | ✅ | |
| **Attack Time Heatmap** | grid day × 3h | `GET /operations/reports/attack-time` | date_histogram event_time fixed_interval=3h × day | ✅ | timezone=Asia/Bangkok |
| **Time-Based Logs (table)** | per-event row | `GET /operations/reports/attack-time` | warehouse docs + filter + sort | ✅ | columns: Timestamp/Severity/Threat Types/IOC/Sources/Target/Sources/Action |

### Page 2: Threat Trend

| Label บนหน้าเว็บ | ที่แสดงเป็นอะไร | API endpoint | สูตรคำนวณ | ถูกไหม | หมายเหตุ |
|----------------|----------------|--------------|----------|--------|---------|
| **ThreatTrendSummaryCards** (Total Threat / Critical IOCs / High IOCs / etc.) | reuse จาก Executive dashboard | `GET /executive/dashboard` | ดู Phase 3.1 | ✅ | |
| **Attack Volume Trend** (line chart) | reuse จาก Executive | `GET /executive/dashboard` | date_histogram + severity sub-aggs + Holt-Winters forecast | ✅ | |
| **Raw Volume Logs (table)** | per (hour × sector × threat_type) row | `GET /threat-intelligence/trend/events` | load up to 10K docs → group ใน Python by (hour bucket, sector, threat_type) | ⚠️ | performance concern at scale (10K hard cap) — Phase 2 follow-up |
| Columns: Timestamp / Sectors / Top Threat Type / Critical / High / Medium / Low / Total | per-group counts | same | severity_counts per group | ✅ | |

### Page 3: Attack Origin

| Label บนหน้าเว็บ | ที่แสดงเป็นอะไร | API endpoint | สูตรคำนวณ | ถูกไหม | หมายเหตุ |
|----------------|----------------|--------------|----------|--------|---------|
| **ThreatMap** (world map) | reuse จาก Executive `attack_origin_map.origins` | `GET /executive/dashboard` | top 10 ของ geo_country | ⚠️ | coverage 0.4% |
| **TrendComparisonComponents** | period delta | `GET /operations/reports/attack-origins` | _comparison_metric current vs previous window | ✅ | |
| **Detailed Ranking (table)** | top 10 countries ranking | `GET /operations/reports/attack-origins` | terms `geo_country` + severity sub-aggs + share_percent | ⚠️ | filter Thailand/None ออก, coverage ต่ำ |
| Columns: No./Change/Country/Main Threat Type/Severity DIST./Total Events/Share | per-country | same | per-bucket data | ✅ | |

### Page 4: Threat Type

| Label บนหน้าเว็บ | ที่แสดงเป็นอะไร | API endpoint | สูตรคำนวณ | ถูกไหม | หมายเหตุ |
|----------------|----------------|--------------|----------|--------|---------|
| **Left panel: threat type list** | top N threat types | `GET /operations/reports/threat-types` | terms `ai_threat_types` | ⚠️ | data ปนกัน Title Case + snake_case → list อาจมี duplicate label |
| **Right panel: IOC Type Distribution** | sub-aggs per threat | `GET /operations/reports/threat-types/{type}` | terms `ioc_type` filtered by threat_type | ✅ | |
| **Right panel: Trend** | timeline | `GET /operations/reports/threat-types/{type}` | date_histogram filtered | ✅ | |
| **Right panel: Targeted Sectors** | top sectors | `GET /operations/reports/threat-types/{type}` | terms `target_sector_name` filtered | ⚠️ | coverage 2% |
| **Right panel: Related Attackers** | top actors | `GET /operations/reports/threat-types/{type}` | terms `ai_threat_actors` filtered | ⚠️ | data quality ต่ำ |

### Page 5: Sector Threat

| Label บนหน้าเว็บ | ที่แสดงเป็นอะไร | API endpoint | สูตรคำนวณ | ถูกไหม | หมายเหตุ |
|----------------|----------------|--------------|----------|--------|---------|
| **Most Targeted Sector** (card) | top 1 sector | `GET /operations/reports/target-sectors` | top 1 ของ terms `target_sector_name` | ⚠️ | coverage 2% |
| **Highest Critical Risk** (card) | sector ที่มี critical สูงสุด | same | sub-agg severity=critical per sector → max | ✅ | |
| **Highest Spike Sector** (card) | sector ที่ volume เพิ่ม % สูงสุด | same | delta % current vs previous window | ✅ | |
| **SectorRiskMatrixComponent** | heatmap | same | sector × severity matrix | ✅ | |
| **Targeted Assets by Sector (table)** | per-sector row | same | terms `target_sector_name` + sub fields | ⚠️ | coverage 2%, frontend ส่ง `report_key='sector'` ผ่าน `REPORT_KEY_ALIASES["sector"]="target-sectors"` ✅ |

### Page 6: Intelligence Source

| Label บนหน้าเว็บ | ที่แสดงเป็นอะไร | API endpoint | สูตรคำนวณ | ถูกไหม | หมายเหตุ |
|----------------|----------------|--------------|----------|--------|---------|
| **ThreatIntelligenceSourceChart** (bar) | top sources | `GET /operations/reports/intelligence-sources` | terms `source_name` size 25 → top 5 chart | ✅ | warehouse only — **ไม่มี `_index` leak** (ต่างจาก Threat Landscape) |
| **SeverityDistributionComponent** | severity breakdown per source | same | sub-agg severity per source bucket | ✅ | |
| **TrendComparisonComponents** | trend per source | same | date_histogram per source + previous window | ✅ | |
| **Source Performance (table)** | per-source row | same | terms `source_name` + severity ratio | ⚠️ | data 99.99% เป็น cyberint — แสดง 1 row ใหญ่จริง |
| Columns: Rank/Source/Total Ingested/Critical & High Ratio | per-bucket | same | severity_counts (critical+high) / total | ✅ | |

## Bugs ที่พบ

| # | Severity | Label ที่กระทบ | API endpoint | ปัญหา | สถานะ |
|---|----------|---------------|--------------|------|------|
| _(ไม่มี bug ใหม่ใน Phase 3.6)_ | — | — | — | mapping label↔backend field ตรงทุก dimension. Backend ใช้ warehouse only (ไม่ leak `_index` like Threat Landscape เพราะไม่ merge datalake) | — |

### Bug จาก Phase 2/3 ที่กระทบหน้านี้ (แก้แล้ว)

| # | Severity | Label ที่กระทบ | สถานะ |
|---|----------|---------------|------|
| BUG-2.3-1 | CRITICAL | filter sources/threat_types/severities บน reports endpoints | ✅ แก้ Phase 2.3 — filter plumbing ครบทุก endpoint |
| BUG-3.0-5 | HIGH | ตัวเลขรวม (Total Threat/Critical) ที่ reuse Executive | ✅ แก้ Phase 3.0 — warehouse_eligible_only opt-out |

## ข้อสังเกต / Data quality

| # | ประเด็น | ผลต่อหน้าเว็บ |
|---|--------|---------------|
| 1 | **Attack Origin: geo_country 99% None/MISSING** | "Detailed Ranking" ranking มาจาก 0.4% ของ data — top 10 อาจไม่ representative |
| 2 | **Sector Threat: target_sector_name 97.9% MISSING** | "Most Targeted Sector" + "Targeted Assets by Sector" ranking มาจาก 2% ของ data |
| 3 | **Threat Type: ai_threat_types vocab ปนกัน** (Title Case + snake_case) | Left panel list อาจแสดง entries ซ้ำสำหรับความหมายเดียวกัน |
| 4 | **Intelligence Source: cyberint ครอง 99.99%** | "Source Performance" table แสดง row หลักเป็น Cyberint — chart ไม่ค่อย informative |
| 5 | **Threat Trend Raw Volume Logs: load up to 10K docs → group ใน Python** | high-load query อาจช้า — performance follow-up |
| 6 | **Frontend `getReportKeyFromSegment('4')` fallback ไป "sector"** สำหรับ Sector Threat page | semantic OK (mapping ทำงาน) แต่ logic ซับซ้อน — ไม่กระทบ backend |
| 7 | **Intelligence Source endpoint ไม่ merge datalake** เหมือน Threat Landscape | ดีกว่า — ไม่มี `_index` leak issue (BUG-3.4-1 ไม่กระทบหน้านี้) |

## ที่ต้องตรวจสอบเพิ่มเติม (pending)

- [ ] **Performance follow-up:** `/threat-intelligence/trend/events` load 10K docs → group ใน Python อาจช้าที่ scale ใหญ่ — เสนอ ES bucket script agg แทน
- [ ] **Spot-check ตัวเลขจริง:** ใน production data, top countries / top sectors / top sources ตรง expectation ไหม

## Regression tests

ยังไม่มี — Phase 3.6 ไม่พบ semantic bug ใหม่

## Commits ที่เกี่ยวข้อง

- Phase 2.3: filter plumbing (`sources`/`threat_types`/`severities`)
- Phase 3.0: warehouse_eligible_only opt-out
- Phase 3.6: _ไม่มี commit fix_ — เป็นการ audit + document mapping เท่านั้น
