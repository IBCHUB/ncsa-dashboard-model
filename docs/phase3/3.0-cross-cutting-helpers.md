# 3.0 — Cross-cutting ES query helpers

> **หมายเหตุ:** 3.0 เป็น **foundation audit** ไม่มี UI page — ตรวจ ES query helper ที่ endpoint อื่น 12+ ตัวพึ่งพา (bug ที่นี่กระทบทุกหน้า)
> ดังนั้นคอลัมน์ "Label ที่หน้าเว็บ" ใน semantic map จะใช้ "ชื่อ Helper" แทน

## หน้า/Routes ที่ตรวจ

- **Frontend:** (ไม่มี — เป็น helper audit)
- **API endpoints:** (ไม่มี — helper ถูกเรียกจาก endpoint อื่น)
- **Backend file:** `ai-service/services/dashboard_router.py`

## ตารางตรวจ Helper (semantic map)

| Helper / Constant | บรรทัด | Backend function | ES field/index ที่ใช้ | ความหมาย / สูตรคำนวณ | Verified | Bug? |
|-------------------|--------|------------------|----------------------|---------------------|----------|------|
| `_warehouse_dashboard_aggs` | ~1802 | shared agg builder | warehouse (.43) — `severity`, `geo_country`, `target_sector_name`, `ai_threat_types`, `source_name`, `last_seen`, ... | สร้าง aggregate body ใช้กับ 12+ endpoint (executive, operations, IOC summary, threat type, sector, source, attack origin) — รวมทั้ง terms agg + previous-period delta | ✅ | ไม่มี (BUG-2.3 plumbing แก้ไปแล้วใน Phase 2.3) |
| `_warehouse_summary_stats` | 1358 | summary builder | warehouse (.43) — `severity`, `geo_country`, `tlp` | นับ severity_counts, critical_active, high_active, thailand_threat สำหรับหน้า executive/operations | ✅ | **BUG-3.0-5** (HIGH) — silently filter `warehouse_eligible=true` ทิ้ง rejected docs |
| `_warehouse_search_filters` | ~1130 | filter builder | warehouse (.43) | สร้าง bool filter list จาก start_date / end_date / sources / threat_types / severities / risk_levels / ioc_types / time_mode | ✅ | ไม่มี (แก้แล้วใน Phase 2.1d) |
| `_search_warehouse_docs` / `_scroll_all_warehouse_docs` | — | doc fetcher | warehouse (.43) | ดึง doc list ตาม filter; `_scroll_all_*` ใช้สำหรับ aggregation in-Python | ✅ | ไม่มี |
| `_datalake_dashboard_aggs` | ~5400 | datalake agg builder | datalake (.41) `tcti-feeds` — `severity` (numeric), `_index` | ใช้กับ IOC summary `tab=statistics-import` เท่านั้น | ⚠️ | **BUG-3.0-9** (MED) — aggregate `_index` แทน `source_name` |
| `_normalize_severity` | 481 | severity normalizer | input: string หรือ numeric | string → string mapping (`critical/high/medium/low/clean`); numeric (Cyberint) → bin: 80+/60+/40+/0/else | ✅ | ไม่มี |
| `_CYBERINT_SEVERITY_BANDS` | 52 | inverse mapping | output: numeric range | inverse ของ `_normalize_severity`: ใช้แปลง string severity → numeric range สำหรับ datalake query | ✅ | ไม่มี (auditor's finding ผิด) |
| `WAREHOUSE_TIME_FIELDS` | 68 | time-mode → field list | — | mode "observed" → `[event_time, first_seen, last_seen]`; "processed" → `[processed_at, created_at, collect_time]`; "published" → `[published_at]`; **"changed" → `[last_shared_at, action_updated_at]`** (เดิม `[revoked_at, last_shared_at, updated_at]` แต่ 2 field ไม่มีจริง) | ✅ | **BUG-3.0-1** (CRITICAL) — แก้แล้ว |
| `DATALAKE_TIME_FIELDS` | 75 | time-mode → field list | — | mode "observed" → `[observation_date, first_seen]`; "processed" → `[@timestamp, processed_at]`; "changed" → `[@timestamp]` (fallback) | ✅ | ไม่มี |
| `PYTHON_FILTER_FIELDS` | 85 | Python-side filter field list | — | ใช้ใน Python หลังดึง doc แล้ว — mode "changed" เดิมอ้าง field ที่ไม่มี | ✅ | **BUG-3.0-1** (CRITICAL) — แก้แล้ว |
| `thailand_threat` agg (in `_warehouse_summary_stats`) | 1394 | Thailand count | warehouse (.43) `geo_country` (keyword, ISO-2 codes) | นับ doc ที่ `geo_country == "TH"` (เดิมมี clause `"Thailand"`/`"thailand"` ที่ตาย เพราะ field เก็บ ISO codes เท่านั้น) | ✅ | **BUG-3.0-7** (MED) — แก้แล้ว |

## Bugs ที่พบ

| # | ระดับ | สรุป | Commit ของ fix |
|---|-------|------|---------------|
| BUG-3.0-1 | CRITICAL | `WAREHOUSE_TIME_FIELDS["changed"]` และ `PYTHON_FILTER_FIELDS["changed"]` อ้าง field `revoked_at`, `updated_at` ที่ไม่อยู่ใน mapping → เปลี่ยนเป็น `last_shared_at` + `action_updated_at` | `a578e50` |
| BUG-3.0-5 | HIGH | `_warehouse_summary_stats` AND-filter `warehouse_eligible=true` แบบเงียบ ทิ้ง 372K rejected docs → เพิ่ม parameter `warehouse_eligible_only: bool = True` ให้ opt-out ได้ | `a578e50` |
| BUG-3.0-7 | MED | `thailand_threat` agg มี clause ตาย `"Thailand"`/`"thailand"` → ลดเหลือ `term: geo_country=TH` เพียงตัวเดียว | `a578e50` |

### รายละเอียดการแก้ (diff)

#### Fix 1 — ลบ phantom date fields

```diff
 WAREHOUSE_TIME_FIELDS: dict[str, list[str]] = {
     "observed": ["event_time", "first_seen", "last_seen"],
     "processed": ["processed_at", "created_at", "collect_time"],
     "published": ["published_at"],
-    "changed": ["revoked_at", "last_shared_at", "updated_at"],
+    "changed": ["last_shared_at", "action_updated_at"],
 }

 PYTHON_FILTER_FIELDS: dict[str, list[str]] = {
     "observed": ["event_time", "first_seen", "last_seen", "observation_date"],
     "processed": ["processed_at", "created_at", "collect_time"],
     "published": ["published_at"],
-    "changed": ["revoked_at", "last_shared_at", "updated_at"],
+    "changed": ["last_shared_at", "action_updated_at"],
 }
```

**หลักฐาน:** `es_mapping_warehouse.json` — search หา `revoked_at` และ `updated_at` ไม่เจอ
**ผลกระทบเดิม:** caller ใดที่ส่ง `time_mode="changed"` จะได้ date range OR ของ 3 field ซึ่ง 2 ใน 3 ไม่เคยมีจริง ES treat เป็น missing เงียบๆ → filter ทำงานจริงผ่าน `last_shared_at` ตัวเดียว ตอนนี้ explicit แล้ว

#### Fix 5 — เพิ่ม `warehouse_eligible_only` parameter

```diff
 def _warehouse_summary_stats(
     start_date: Optional[str],
     end_date: Optional[str],
     time_mode: str = TIME_MODE_PROCESSED,
     *,
     sources: Optional[List[str]] = None,
     threat_types: Optional[List[str]] = None,
     severities: Optional[List[str]] = None,
     ioc_types: Optional[List[str]] = None,
+    warehouse_eligible_only: bool = True,
 ) -> Dict[str, Any]:
```

**ทำไม:** ของเดิม hard-code `warehouse_eligible_only=True` → AND filter `warehouse_eligible=true` เงียบๆ ทิ้ง 372,389 rejected docs ทั้งๆ ที่ caller บางตัวอยากนับ total รวม
**Default คงเดิม** — preserve behavior เดิม เปิดประตูให้ opt-out ได้เท่านั้น

#### Fix 7 — ลบ Thailand clauses ตายแล้ว

```diff
 "thailand_threat": {
-    "filter": {
-        "bool": {
-            "should": [
-                {"term": {"geo_country": "Thailand"}},
-                {"term": {"geo_country": "TH"}},
-                {"term": {"geo_country": "thailand"}},
-            ],
-            "minimum_should_match": 1,
-        }
-    },
+    "filter": {"term": {"geo_country": "TH"}},
 },
```

**หลักฐาน:** `warehouse_field_distribution.json` — `geo_country` มีเฉพาะ ISO-2 codes บวก `None` literal + MISSING ไม่มีคำว่า `Thailand` เลย

## เลื่อนทำ / Data quality notes

| # | ระดับ | ปัญหา | เหตุผลที่เลื่อน |
|---|-------|------|---------------|
| BUG-3.0-2 | (auditor's finding ผิด) | `_CYBERINT_SEVERITY_BANDS["high"]=(60,79)` "ตาย" | Revert — band table เป็น inverse ของ `_normalize_severity` numeric branch ตั้งใจให้ครอบคลุม numeric input ทุกแหล่ง ไม่ใช่แค่ Cyberint |
| BUG-3.0-3 | HIGH (landmine) | `tlp` field เป็น `text+keyword` — ES `term`/`terms` บน bare `tlp` คืน 0 silently | ปัจจุบันไม่มี ES-side filter บน `tlp`; ทำใน Python ทั้งหมด — flag เป็น future landmine |
| BUG-3.0-4 | HIGH (verified ไม่ใช่ bug) | `target_sector_name` agg อาจคืนค่าว่าง | Distribution ยืนยัน coverage = `target_sector` (~2% docs) เท่ากัน ไม่ใช่ bug |
| BUG-3.0-6 | MED | Severity sub-agg ใช้ `"missing": "low"` | Distribution แสดง 0 missing severities — impact = 0 |
| BUG-3.0-8 | MED | `ai_threat_types` agg vocabulary ปนกัน (Title Case + snake_case) | Data quality issue ไม่ใช่ query bug — Phase 1 ingestion territory |
| BUG-3.0-9 | MED | `_datalake_dashboard_aggs` aggregate `_index` แทน `source_name` | ต้อง verify datalake mapping ว่ามี `source_name.keyword` ก่อน — flag ไว้ |
| BUG-3.0-10 | LOW | ไม่มี server-side `tlp != red` guard ใน `_warehouse_search_filters` | ข้อมูล 100% amber วันนี้; Python-side filter ทำงานสำหรับ action endpoints |
| BUG-3.0-11 | LOW | Cardinality default threshold (3000) | ไม่มี impact |
| BUG-3.0-12 | LOW | audit-map อ้าง "3 datalake endpoints" แต่ grep เจอ 5+ refs | จะ re-verify ตอน 3.3 (TI overview/IOC summary) และ 3.4 (threat landscape) |

## Regression tests (`tests/semantic/test_phase_3_0_helpers.py`)

| Test | ตรวจอะไร |
|------|---------|
| `test_warehouse_time_fields_only_reference_real_warehouse_fields` | ทุก field ใน `WAREHOUSE_TIME_FIELDS` ต้องอยู่ใน mapping จริง |
| `test_python_filter_fields_changed_does_not_reference_phantom_fields` | เหมือนกันสำหรับ `PYTHON_FILTER_FIELDS["changed"]` |
| `test_cyberint_severity_bands_mirror_normalize_severity` | Band table เป็น inverse ของ `_normalize_severity` numeric branch (round-trip consistency) |
| `test_warehouse_summary_stats_accepts_warehouse_eligible_only` | Helper เปิดเผย parameter; default คง behavior เดิม |
| `test_thailand_filter_uses_only_iso_code` | Source ไม่มี variant "Thailand" หรือ "thailand" แล้ว |

ทั้ง 5 ผ่าน ไม่มี regression ใหม่ใน suite อื่น (11 pre-existing failures เท่าเดิม)

## Commits ที่เกี่ยวข้อง

- `1c55556` — capture ES ground-truth fixtures (mappings + samples + distributions)
- `a578e50` — phase-3.0 helper semantic fixes (Fixes 1, 5, 7) + 5 regression tests
