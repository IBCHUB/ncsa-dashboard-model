# 3.0 — สูตรกลาง (helper) ที่หลายหน้าใช้ร่วมกัน

> **3.0 ไม่มีหน้าเว็บโดยตรง** — ตรวจสูตรกลางที่ใช้ใน 12+ หน้า ถ้าตรงนี้ผิด ทุกหน้าจะเห็นตัวเลขผิดตาม

## หน้าเว็บที่ตรวจ

- **ชื่อหน้า:** ทุกหน้าที่ใช้สูตรเหล่านี้ (รายละเอียดในตาราง "Bugs ที่พบ")
- **URL:** ครอบคลุม — Executive, Operations, Threat Intelligence, IOC pages ทั้งหมด

## รายการสูตรกลางที่ตรวจ

| สูตร/Helper | ใช้คำนวณตัวเลขประเภทไหน | ใครใช้ | ถูกไหม |
|------------|-----------------------|--------|--------|
| ตัวเลขแยกตาม severity (critical / high / medium / low / clean) | จำนวน IOC แยกตามความรุนแรง | Executive, Operations, IOC Summary, ทุกหน้าที่มี severity bar | ✅ |
| ตัวเลขแยกตามประเทศ | "Top Attack Origins", map | Executive, Operations, Threat Landscape | ✅ |
| ตัวเลขแยกตาม sector | "Target Sectors" | Executive, Operations | ⚠️ (97.9% MISSING — แสดงจาก doc 2%) |
| ตัวเลขแยกตามประเภทภัยคุกคาม | "Threat Types" | Executive, Operations, TI | ⚠️ (ค่าใน data ปนกัน Title Case + snake_case) |
| ตัวเลขแยกตามแหล่งข้อมูล | "Intelligence Sources" | Executive, Operations | ⚠️ (cyberint 99.99% — แสดงคนเดียวจริงๆ) |
| การกรองตามช่วงเวลา "Observed" | filter by event/seen time | ทุกหน้าที่มี date picker เป็น Observed mode | ✅ |
| การกรองตามช่วงเวลา "Processed" | filter by processing time | ทุกหน้าที่มี date picker เป็น Processed mode | ✅ |
| การกรองตามช่วงเวลา "Changed" | filter by change time | หน้า Action Center, External Sharing | **❌ เคยอ้าง field ที่ไม่มีจริง** (แก้แล้ว — BUG-3.0-1) |
| ตัวเลข "Active IOC" / รวม | นับ docs ทั้งหมดในเงื่อนไข | Executive, Operations | **❌ เคยทิ้ง rejected docs เงียบๆ** (แก้แล้ว — BUG-3.0-5) |
| ตัวเลขภัยคุกคามที่กระทบประเทศไทย | "Thailand Threat" badge | Executive (summary), Operations | **❌ เคย filter แบบที่ field เก็บไม่ได้** (แก้แล้ว — BUG-3.0-7) |

## Bugs ที่พบ

| # | Severity | Label ที่กระทบ | ปัญหา | สถานะ |
|---|----------|---------------|------|------|
| BUG-3.0-1 | CRITICAL | ทุกหน้าที่ filter ตาม "Changed" date mode (Action Center, External Sharing) | สูตรอ้าง field `revoked_at` กับ `updated_at` ที่ **ไม่มีอยู่ใน warehouse จริง** → date filter ใช้ field เดียวเฉยๆ (last_shared_at) เงียบๆ ไม่ error | ✅ แก้แล้ว (commit `a578e50`) — เปลี่ยนเป็น field ที่มีจริง |
| BUG-3.0-5 | HIGH | Executive Dashboard "Total Threats" / summary numbers ทุก endpoint | สูตรสรุปยอดรวม **ทิ้ง rejected docs 372K เงียบๆ** (3.4% ของข้อมูล) → ตัวเลขรวมที่หน้าเว็บแสดง อาจน้อยกว่าจริง | ✅ แก้แล้ว (commit `a578e50`) — เปิดทาง opt-out รวม rejected ได้ตามต้องการ |
| BUG-3.0-7 | MED | Executive summary "Thailand Threat" badge (ตัวเลขภัยคุกคามที่กระทบไทย) | สูตร filter หาคำว่า "Thailand" และ "thailand" แต่ field เก็บแค่ ISO code "TH" → 2 clause ไม่มีทาง match (ไม่กระทบผลลัพธ์ — clause ที่ถูก "TH" ทำงานคู่กัน) | ✅ แก้แล้ว (commit `a578e50`) — ลดเหลือ "TH" อย่างเดียว |

## ข้อสังเกต / Data quality

| # | ประเด็น | ผลต่อหน้าเว็บ |
|---|--------|---------------|
| 1 | `severity` ในข้อมูลจริง: low 11M / medium 40K / critical 8.6K / **high 1 doc** | บัตเชอวร์ "High" บนทุกหน้าจะแสดงตัวเลขใกล้ศูนย์เสมอ (ไม่ใช่ bug ของ code) |
| 2 | `tlp = amber` 100% ของข้อมูล | TLP gating (Phase 2) ทำงานถูก แต่ยังไม่มีข้อมูล non-amber ให้กรองจริง |
| 3 | `geo_country` 99.4% เป็น "None"/MISSING — ค่าประเทศจริงแค่ 0.4% | "Top Attack Origins" บน Executive/Operations แสดงจาก data ส่วนน้อยมาก — ไม่ใช่ bug แต่เป็น data coverage |
| 4 | `target_sector` 97.9% MISSING — ค่าจริงแค่ 2% | "Target Sectors" chart มีข้อมูลแค่ 2% — coverage ต่ำ |
| 5 | `source_name` 99.99% เป็น cyberint_iocs | "Top Intelligence Sources" = Cyberint แทบจะอย่างเดียว — chart ไม่ informative |
| 6 | `ai_threat_types` ปนกัน Title Case (Malware, Phishing) กับ snake_case (malware_payload, phishing_website) | "Threat Types" chart อาจแสดง 2 แท่งสำหรับความหมายเดียวกัน (e.g., Phishing 894 + phishing_website 364K) |
| 7 | `action_status` มีแค่ open กับ MISSING (ไม่มี closed/in_progress) | Action Center workflow ยังไม่ได้ใช้งานจริง |
| 8 | `reviewed_at` field มีอยู่ใน mapping แต่ไม่มี doc ไหนใส่ค่าเลย | ฟีเจอร์ review workflow ยังไม่ทำงาน |

## ตรวจสอบความถูกต้องของการแก้ (regression tests)

ไฟล์: `ai-service/tests/semantic/test_phase_3_0_helpers.py` (5 tests)

ทุก test ผ่าน — ไม่ทำให้ test เดิมพังเพิ่ม (pre-existing failures 11 ตัวเท่าเดิม)

## Commits

- `1c55556` — บันทึก ground-truth fixtures จาก ES จริง (warehouse + datalake mapping + sample docs + field distributions)
- `a578e50` — แก้ Bugs 3 ตัว + เพิ่ม regression tests 5 ตัว
