# 3.0 — Cross-cutting ES query helpers

> **หมายเหตุ:** 3.0 เป็น **foundation audit** ไม่มี UI page — ตรวจ ES query helper ที่ endpoint อื่น 12+ ตัวพึ่งพา (bug ที่นี่กระทบทุกหน้า)
> ดังนั้นคอลัมน์ "UI label" ใน Label inventory จะใช้ "ชื่อ Helper" แทน

## Routes audited

- **Frontend (.tsx):** (ไม่มี — เป็น helper audit)
- **API endpoints:** (ไม่มี — helper ถูกเรียกจาก endpoint อื่น 12+ ตัว)
- **Backend file:** `ai-service/services/dashboard_router.py`

## Label inventory

| UI label *(= Helper)* | Response path | Backend fn | ES field | ES agg | สูตรคำนวณ / ความหมาย | Verified | Bug? |
|----------------------|---------------|------------|----------|--------|---------------------|----------|------|
| `_warehouse_dashboard_aggs` | — | shared agg builder (line ~1802) | warehouse (.43) `severity`, `geo_country`, `target_sector_name`, `ai_threat_types`, `source_name`, `last_seen` | terms + date_histogram + previous-period delta | สร้าง aggregate body ที่ 12+ endpoint ใช้ร่วมกัน (executive, operations, IOC summary, threat type, sector, source, attack origin) | ✅ | — |
| `_warehouse_summary_stats` | — | summary builder (line 1358) | warehouse (.43) `severity`, `geo_country`, `tlp` | filters agg (severity_counts) + 3 single-filter aggs (critical_active, high_active, thailand_threat) | นับ docs ต่อ severity + critical/high active + Thailand-targeted; รองรับ start/end date / time_mode / filters | ✅ | **BUG-3.0-5** (HIGH) — silently ทิ้ง rejected docs |
| `_warehouse_search_filters` | — | filter builder (line ~1130) | warehouse (.43) | bool filter list | สร้าง filter จาก start_date / end_date / sources / threat_types / severities / risk_levels / ioc_types / time_mode | ✅ | — |
| `_search_warehouse_docs` / `_scroll_all_warehouse_docs` | — | doc fetcher | warehouse (.43) | (no agg — fetch only) | ดึง doc list ตาม filter; `_scroll_all_*` ใช้สำหรับ aggregation in-Python | ✅ | — |
| `_datalake_dashboard_aggs` | — | datalake agg builder (line ~5400) | datalake (.41) `tcti-feeds` `severity` (numeric), `_index` | terms agg | ใช้กับ IOC summary `tab=statistics-import` เท่านั้น | ⚠️ | **BUG-3.0-9** (MED, deferred) — agg `_index` แทน `source_name` |
| `_normalize_severity` | — | severity normalizer (line 481) | input: string หรือ numeric | (mapping) | string → string (`critical/high/medium/low/clean`); numeric (Cyberint) → bin: 80+→critical / 60+→high / 40+→medium / 0→clean / else→low | ✅ | — |
| `_CYBERINT_SEVERITY_BANDS` | — | inverse mapping (line 52) | output: numeric range | (constant table) | inverse ของ `_normalize_severity` numeric branch: ใช้แปลง string severity → numeric range สำหรับ datalake query (critical: 80-100, high: 60-79, medium: 40-59, low: 1-39, clean: 0-0) | ✅ | — |
| `WAREHOUSE_TIME_FIELDS` | — | time-mode → field list (line 68) | — | — | mode "observed" → `[event_time, first_seen, last_seen]`; "processed" → `[processed_at, created_at, collect_time]`; "published" → `[published_at]`; **"changed" → `[last_shared_at, action_updated_at]`** (เดิม `[revoked_at, last_shared_at, updated_at]` แต่ 2 field ไม่มีจริง) | ✅ | **BUG-3.0-1** (CRITICAL) — แก้แล้ว |
| `DATALAKE_TIME_FIELDS` | — | time-mode → field list (line 75) | — | — | mode "observed" → `[observation_date, first_seen]`; "processed" → `[@timestamp, processed_at]`; "changed" → `[@timestamp]` (fallback) | ✅ | — |
| `PYTHON_FILTER_FIELDS` | — | Python-side filter field list (line 85) | — | — | ใช้ใน Python หลังดึง doc แล้ว — mode "changed" เดิมอ้าง field ที่ไม่มี | ✅ | **BUG-3.0-1** (CRITICAL) — แก้แล้ว |
| `thailand_threat` (agg ใน `_warehouse_summary_stats`) | `summary.thailand_threat.doc_count` | summary builder (line 1394) | warehouse (.43) `geo_country` (keyword, ISO-2) | single-filter agg | `count(docs WHERE geo_country == "TH")` — เดิมมี clause `"Thailand"`/`"thailand"` ที่ตายเพราะ field เก็บ ISO codes เท่านั้น | ✅ | **BUG-3.0-7** (MED) — แก้แล้ว |

## Bugs found

| # | Severity | สรุป (1 บรรทัด) | Fix commit |
|---|----------|----------------|-----------|
| BUG-3.0-1 | CRITICAL | `WAREHOUSE_TIME_FIELDS["changed"]` และ `PYTHON_FILTER_FIELDS["changed"]` อ้าง `revoked_at`+`updated_at` ที่ไม่อยู่ใน mapping → เปลี่ยนเป็น `last_shared_at`+`action_updated_at` | `a578e50` |
| BUG-3.0-5 | HIGH | `_warehouse_summary_stats` AND-filter `warehouse_eligible=true` แบบเงียบ ทิ้ง 372K rejected docs → เพิ่ม parameter `warehouse_eligible_only` ให้ opt-out ได้ | `a578e50` |
| BUG-3.0-7 | MED | `thailand_threat` agg มี clause ตาย `"Thailand"`/`"thailand"` → ลดเหลือ `term: geo_country=TH` ตัวเดียว | `a578e50` |

### รายละเอียดการแก้ (diff)

#### Fix 1 — ลบ phantom date fields (CRITICAL)

```diff
 WAREHOUSE_TIME_FIELDS: dict[str, list[str]] = {
     "observed": ["event_time", "first_seen", "last_seen"],
     "processed": ["processed_at", "created_at", "collect_time"],
     "published": ["published_at"],
-    "changed": ["revoked_at", "last_shared_at", "updated_at"],
+    "changed": ["last_shared_at", "action_updated_at"],
 }

 PYTHON_FILTER_FIELDS: dict[str, list[str]] = {
     "observed": ["event_time", "first_seen", "last_seen", "observation_date"],
     "processed": ["processed_at", "created_at", "collect_time"],
     "published": ["published_at"],
-    "changed": ["revoked_at", "last_shared_at", "updated_at"],
+    "changed": ["last_shared_at", "action_updated_at"],
 }
```

**หลักฐาน:** `es_mapping_warehouse.json` — search หา `revoked_at` และ `updated_at` ไม่เจอ
**ผลกระทบเดิม:** caller ใดที่ส่ง `time_mode="changed"` จะได้ date range OR ของ 3 field ซึ่ง 2 ใน 3 ไม่เคยมีจริง ES treat เป็น missing เงียบๆ → filter ทำงานจริงผ่าน `last_shared_at` ตัวเดียว ตอนนี้ explicit แล้ว

#### Fix 5 — เพิ่ม `warehouse_eligible_only` parameter (HIGH)

```diff
 def _warehouse_summary_stats(
     start_date: Optional[str],
     end_date: Optional[str],
     time_mode: str = TIME_MODE_PROCESSED,
     *,
     sources: Optional[List[str]] = None,
     threat_types: Optional[List[str]] = None,
     severities: Optional[List[str]] = None,
     ioc_types: Optional[List[str]] = None,
+    warehouse_eligible_only: bool = True,
 ) -> Dict[str, Any]:
```

**ทำไม:** ของเดิม hard-code `warehouse_eligible_only=True` → AND filter `warehouse_eligible=true` เงียบๆ ทิ้ง 372,389 rejected docs ทั้งๆ ที่ caller บางตัวอยากนับ total รวม
**Default คงเดิม** — preserve behavior; เปิดประตูให้ opt-out ได้เท่านั้น

#### Fix 7 — ลบ Thailand clauses ตายแล้ว (MED)

```diff
 "thailand_threat": {
-    "filter": {
-        "bool": {
-            "should": [
-                {"term": {"geo_country": "Thailand"}},
-                {"term": {"geo_country": "TH"}},
-                {"term": {"geo_country": "thailand"}},
-            ],
-            "minimum_should_match": 1,
-        }
-    },
+    "filter": {"term": {"geo_country": "TH"}},
 },
```

**หลักฐาน:** `warehouse_field_distribution.json` — `geo_country` มีเฉพาะ ISO-2 codes บวก `None` literal + MISSING ไม่มีคำว่า `Thailand` เลย

## Deferred / data-quality notes

| # | Severity | ปัญหา | เหตุผลที่เลื่อน |
|---|----------|------|---------------|
| BUG-3.0-2 | — (auditor ผิด) | `_CYBERINT_SEVERITY_BANDS["high"]=(60,79)` ถูกอ้างว่า "ตาย" | Revert — band table เป็น inverse ของ `_normalize_severity` numeric branch ตั้งใจครอบคลุม numeric input ทุกแหล่ง ไม่ใช่แค่ Cyberint |
| BUG-3.0-3 | HIGH (landmine) | `tlp` field เป็น `text+keyword` — ES `term`/`terms` บน bare `tlp` คืน 0 silently | ปัจจุบันไม่มี ES-side filter บน `tlp`; ทำใน Python ทั้งหมด — flag เป็น future landmine |
| BUG-3.0-4 | — (verify แล้วไม่ใช่ bug) | `target_sector_name` agg อาจคืนค่าว่าง | Distribution ยืนยัน coverage = `target_sector` (~2% docs) เท่ากัน |
| BUG-3.0-6 | MED | Severity sub-agg ใช้ `"missing": "low"` | Distribution แสดง 0 missing severities — impact = 0 |
| BUG-3.0-8 | MED | `ai_threat_types` agg vocabulary ปนกัน (Title Case + snake_case) | Data quality issue ไม่ใช่ query bug — Phase 1 ingestion territory |
| BUG-3.0-9 | MED | `_datalake_dashboard_aggs` aggregate `_index` แทน `source_name` | ต้อง verify datalake mapping ว่ามี `source_name.keyword` ก่อน |
| BUG-3.0-10 | LOW | ไม่มี server-side `tlp != red` guard ใน `_warehouse_search_filters` | ข้อมูล 100% amber วันนี้; Python-side filter ทำงานอยู่ |
| BUG-3.0-11 | LOW | Cardinality default threshold (3000) | ไม่มี impact |
| BUG-3.0-12 | LOW | audit-map อ้าง "3 datalake endpoints" แต่ grep เจอ 5+ refs | จะ re-verify ตอน 3.3 และ 3.4 |

## Regression tests

ไฟล์: `ai-service/tests/semantic/test_phase_3_0_helpers.py`

| Test | ตรวจอะไร |
|------|---------|
| `test_warehouse_time_fields_only_reference_real_warehouse_fields` | ทุก field ใน `WAREHOUSE_TIME_FIELDS` ต้องอยู่ใน mapping จริง |
| `test_python_filter_fields_changed_does_not_reference_phantom_fields` | เหมือนกันสำหรับ `PYTHON_FILTER_FIELDS["changed"]` |
| `test_cyberint_severity_bands_mirror_normalize_severity` | Band table เป็น inverse ของ `_normalize_severity` numeric branch (round-trip consistency) |
| `test_warehouse_summary_stats_accepts_warehouse_eligible_only` | Helper เปิดเผย parameter; default คง behavior เดิม |
| `test_thailand_filter_uses_only_iso_code` | Source ไม่มี variant "Thailand" หรือ "thailand" แล้ว |

ทั้ง 5 PASS ไม่มี regression ใหม่ใน suite อื่น (pre-existing 11 failures เท่าเดิม)

## Commits ที่เกี่ยวข้อง

- `1c55556` — capture ES ground-truth fixtures (mappings + samples + distributions)
- `a578e50` — phase-3.0 helper semantic fixes (Fixes 1, 5, 7) + 5 regression tests
